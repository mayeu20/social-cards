# kesslernity-ig

Public asset host for @kesslernity Instagram posts. Buffer's API attaches media by direct
file URL, so the rendered reels and carousel frames are parked here for scheduling.

Source and build engine: `~/Desktop/kesslernity/kesslernity-instagram/` (studio.py + content.py).
These are build outputs, not sources. Rebuild rather than edit.
